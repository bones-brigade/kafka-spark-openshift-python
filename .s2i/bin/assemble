#!/bin/bash

echo "Running parent image assemble script"

/usr/libexec/s2i/assemble

if [ -n "$USER_FUNCTION_URI" ]
then
    echo "Downloading user function"
    curl "$USER_FUNCTION_URI" --output /opt/app-root/src/userfunction.py
fi
